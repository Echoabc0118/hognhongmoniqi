# Database
# Required by Drizzle, database scripts, leaderboard, auth, blog, and game records.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require"

# Doubao Seed LLM
# Required by /api/chat and /api/blog/generate.
ARK_API_KEY="replace-with-your-ark-api-key"
DOUBAO_SEED_MODEL="doubao-seed-character-251128"

# Doubao / Volcengine TTS
# Required by /api/tts.
DOUBAO_TTS_API_KEY="replace-with-your-doubao-tts-api-key"
DOUBAO_TTS_ENDPOINT="https://openspeech.bytedance.com/api/v3/tts/unidirectional"
DOUBAO_TTS_RESOURCE_ID="seed-tts-2.0"

# Runtime
# Optional. Used by src/server.ts and layout/dev behavior.
COZE_PROJECT_ENV="DEV"
HOSTNAME="localhost"
PORT="5000"

# Sharing
# Optional. Used when rendering generated game result cards.
COZE_PROJECT_DOMAIN_DEFAULT="http://localhost:5000"

# Supabase
# Optional in the current app flow. Kept here because a Supabase helper exists.
COZE_SUPABASE_URL=""
COZE_SUPABASE_ANON_KEY=""
COZE_SUPABASE_SERVICE_ROLE_KEY=""
